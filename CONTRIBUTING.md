# Contribuire
- Usa `make check` prima del commit.
- Commit convention: tipo(scope): descrizione (es. feat(ansible): add curl task).
- Apri PR con descrizione del problema, log/trace e output LLM se usato.
